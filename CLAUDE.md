# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

> ⚠️ **플레이스홀더 문서입니다.**
> 현재 이 저장소에는 소스 코드가 없습니다 (`.gitignore`, `connection_test.txt`만 존재).
> 실제 코드·빌드 설정·테스트가 추가되면 아래 항목을 채우거나 `/init`을 다시 실행하세요.

## 프로젝트 개요

- **이름**: 삼성증권-0721
- **상태**: 초기 설정 단계 (코드 미작성)
- **플랫폼**: Windows (개발 환경 기준)
- **기본 언어**: 한국어 (커밋·문서·주석은 한국어로 작성)

## 개발 명령어

<!-- 스택 확정 후 채워주세요. 예시:
- 빌드: `npm run build`
- 실행: `npm run dev`
- 린트: `npm run lint`
- 전체 테스트: `npm test`
- 단일 테스트: `npm test -- <파일 또는 테스트명>`
-->

_아직 정의되지 않음._

## 아키텍처

<!-- 여러 파일을 읽어야 파악되는 "큰 그림" 구조를 여기에 정리하세요.
디렉터리별 세부 목록이 아니라, 모듈 간 관계·데이터 흐름·핵심 결정 사항 위주로. -->

_아직 정의되지 않음._

## 규칙 및 참고 사항

- `.gitignore`는 Node(`node_modules/`, `dist/`)와 Python(`__pycache__/`, `.venv/`) 산출물을 모두 무시하도록 설정되어 있음 — 스택이 아직 확정되지 않았음을 시사.
- 비밀 정보(`.env`, `*.pem`, `*.key`)는 커밋 대상에서 제외됨.
